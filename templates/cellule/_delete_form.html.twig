<form method="post" action="{{ path('app_cellule_delete', {'id': cellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cellule.id) }}">
    <button class="btn">Delete</button>
</form>
