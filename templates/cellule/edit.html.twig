{% extends 'base.html.twig' %}

{% block title %}Edit Cellule{% endblock %}

{% block body %}
    <h1>Edit Cellule</h1>

    {{ include('cellule/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cellule_index') }}">back to list</a>

    {{ include('cellule/_delete_form.html.twig') }}
{% endblock %}
